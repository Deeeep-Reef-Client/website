// window.addEventListener("load", (event) => {
//     if (!navigator.userAgent.includes("Windows")) alert("Sorry, DRC currently only supports Microsoft Windows");
//     if (window.matchMedia("(pointer: coarse)").matches) alert("Sorry, DRC currently does not support mobile devices");
// });